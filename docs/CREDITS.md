`preview.png` at the repo root is the Screenrecord menu with the overlay row selected. The Omarchy marketplace plugin card reads that file automatically. Do not duplicate it under `docs/`.

`pip-person.jpg` is a generated indoor webcam stand-in, composited into `pip-place.png` and `pip-zoom.png` so the live camera is never used.
