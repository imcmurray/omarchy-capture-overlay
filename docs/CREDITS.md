`preview.png` (repo root, copied to `docs/preview.png`) is the Screenrecord menu with the overlay row selected. The Omarchy marketplace plugin card reads this root file automatically.

`pip-person.jpg` is a generated indoor webcam stand-in, composited into the pip shots so the live camera is never used. Overlay chrome in `picker-guides.png`, `shape-panel.png`, `pip-place.png`, and `pip-zoom.png` is cropped from the live plugin.
