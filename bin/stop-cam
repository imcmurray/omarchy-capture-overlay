#!/bin/bash
# Best-effort leftover cleanup. The live preview is torn down by unloading
# CamPreview.qml. Older plugin versions left user systemd camera units; those
# units are gone, so this is a no-op kept for callers that still invoke it.
exit 0
